package name.coreycarter.utils;
import name.coreycarter.utils.Graph;
import java.util.*;
//computer 1->computer 2
public class TopologicalSort<T> {
    Graph<T> map = new Graph<T>();
    List<T> L = new ArrayList<>(); 
    //Set<T> unmark = new HashSet<>(map.keySet());
    Set<T> temp = new HashSet<>();
    Set<T> per = new HashSet<>();
    //while (!unmark.isEmpty()) { 
        //T select = unmark.iterator().next();
    //}
}
